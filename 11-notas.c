#include <stdlib.h>
#include <stdio.h>

int main(void)
{
    int valor, nota50 = 50, nota20 = 20, nota10 = 10, nota1 = 1, qtdNotas;

    if ( valor % nota50 == 0 ) {
        qtdNotas = valor / nota50;
    } else if ( valor  )


    return 0;

}
